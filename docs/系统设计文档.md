# 酒店管理系统 - 系统设计文档

## 1. 项目概述

### 1.1 项目背景
本系统是一个基于 SpringBoot + Vue 的前后端分离酒店管理系统，旨在为酒店提供完整的客房预订、入住管理、财务统计等功能。

### 1.2 技术栈

| 层级 | 技术 | 版本 | 说明 |
|------|------|------|------|
| 后端框架 | SpringBoot | 2.7.18 | 快速开发、自动配置 |
| ORM框架 | MyBatis Plus | 3.5.5 | 简化CRUD、分页插件 |
| 安全框架 | Spring Security | 5.7.x | 认证授权 |
| 数据库 | MySQL | 8.0+ | 关系型数据存储 |
| 缓存 | Redis | 7.x | Token缓存、会话管理 |
| 定时任务 | Quartz | 2.3.x | 报表自动生成 |
| 前端框架 | Vue 3 | 3.x | 响应式、组件化 |
| UI组件 | Element Plus | 2.x | 企业级UI组件库 |
| 构建工具 | Vite | 5.x | 快速热更新 |
| 图表 | ECharts | 5.x | 数据可视化 |
| 容器化 | Docker | 24.x | 一键部署 |

### 1.3 项目目录结构

```
hotel-management/
├── backend/                          # 后端项目
│   ├── src/main/java/com/hotel/
│   │   ├── common/                   # 通用类（Result、异常、常量）
│   │   ├── config/                   # 配置类（CORS、Swagger、Redis）
│   │   ├── controller/               # 控制器层
│   │   ├── dto/                      # 数据传输对象
│   │   ├── entity/                   # 实体类
│   │   ├── job/                      # 定时任务
│   │   ├── mapper/                   # 数据访问层
│   │   ├── security/                 # 安全相关（JWT、权限）
│   │   └── service/                  # 业务逻辑层
│   └── src/main/resources/
│       ├── application.yml           # 主配置文件
│       ├── db/                       # 数据库脚本
│       └── mapper/                   # MyBatis XML映射
├── frontend/                         # 前端项目
│   ├── src/
│   │   ├── api/                      # API接口封装
│   │   ├── layout/                   # 布局组件
│   │   ├── router/                   # 路由配置
│   │   ├── store/                    # 状态管理
│   │   ├── styles/                   # 全局样式
│   │   └── views/                    # 页面组件
│   └── vite.config.js                # Vite配置
├── database/                         # 完整数据库脚本
├── docs/                             # 项目文档
└── docker-compose.yml                # Docker编排文件
```

---

## 2. 系统架构设计

### 2.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        客户端层                              │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐        │
│  │   PC    │  │  平板   │  │  手机   │  │   ...   │        │
│  └────┬────┘  └────┬────┘  └────┬────┘  └────┬────┘        │
└───────┼────────────┼────────────┼────────────┼──────────────┘
        │            │            │            │
        └────────────┴─────┬──────┴────────────┘
                           │
┌──────────────────────────┼──────────────────────────────────┐
│                     Nginx 反向代理                           │
│                    (负载均衡/静态资源)                        │
└──────────────────────────┼──────────────────────────────────┘
                           │
        ┌──────────────────┴──────────────────┐
        │                                      │
┌───────┴───────┐                    ┌────────┴────────┐
│   前端应用     │                    │    后端API      │
│   Vue 3       │                    │   SpringBoot    │
│   Port: 80    │                    │   Port: 8080    │
└───────────────┘                    └────────┬────────┘
                                              │
                    ┌─────────────────────────┼─────────────────────────┐
                    │                         │                         │
            ┌───────┴───────┐        ┌───────┴───────┐        ┌───────┴───────┐
            │    MySQL      │        │    Redis      │        │   Quartz      │
            │   数据存储     │        │   Token缓存   │        │   定时任务    │
            │  Port: 3306   │        │  Port: 6379   │        │   报表生成    │
            └───────────────┘        └───────────────┘        └───────────────┘
```

### 2.2 后端分层架构

```
┌─────────────────────────────────────────────────────────────┐
│                     Controller 层                            │
│              (接收请求、参数校验、返回响应)                    │
├─────────────────────────────────────────────────────────────┤
│                      Service 层                              │
│                (业务逻辑处理、事务管理)                        │
├─────────────────────────────────────────────────────────────┤
│                      Mapper 层                               │
│                   (数据库访问、SQL映射)                       │
├─────────────────────────────────────────────────────────────┤
│                      Entity 层                               │
│                     (实体类定义)                              │
└─────────────────────────────────────────────────────────────┘
```

---

## 3. 权限控制设计 (RBAC模型)

### 3.1 RBAC 模型结构

```
┌─────────┐       ┌─────────┐       ┌─────────┐
│  用户   │ N:M   │  角色   │ N:M   │  菜单   │
│  User   │───────│  Role   │───────│  Menu   │
└─────────┘       └─────────┘       └─────────┘
     │                 │                 │
     │                 │                 │
     ▼                 ▼                 ▼
┌─────────┐       ┌─────────┐       ┌─────────┐
│sys_user │       │sys_role │       │sys_menu │
└─────────┘       └─────────┘       └─────────┘
         \           │           /
          \          │          /
           ▼         ▼         ▼
      ┌──────────────────────────┐
      │    sys_user_role         │
      │    sys_role_menu         │
      │      (关联表)            │
      └──────────────────────────┘
```

### 3.2 权限验证流程

```
┌──────────┐     ┌──────────┐     ┌──────────┐     ┌──────────┐
│  用户    │────▶│  登录    │────▶│ 生成JWT  │────▶│ 存Redis  │
│  请求    │     │  验证    │     │  Token   │     │  缓存    │
└──────────┘     └──────────┘     └──────────┘     └──────────┘
                                                        │
┌──────────┐     ┌──────────┐     ┌──────────┐         │
│  返回    │◀────│  执行    │◀────│  权限    │◀────────┘
│  结果    │     │  业务    │     │  校验    │
└──────────┘     └──────────┘     └──────────┘
```

### 3.3 JWT Token 结构

```json
{
  "header": {
    "alg": "HS512",
    "typ": "JWT"
  },
  "payload": {
    "sub": "admin",
    "userId": 1,
    "username": "admin",
    "exp": 1770436062,
    "iat": 1770349662
  },
  "signature": "..."
}
```

### 3.4 角色权限配置

| 角色 | 权限范围 |
|------|----------|
| 超级管理员 (admin) | 所有功能 (*:*:*) |
| 前台人员 (reception) | 房间、预订、入住、客户、财务管理 |
| 普通用户 (user) | 首页、预订功能 |

---

## 4. 异常处理机制

### 4.1 全局异常处理架构

```java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    // 业务异常处理
    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBusinessException(BusinessException e) {
        return Result.error(e.getCode(), e.getMessage());
    }
    
    // 参数校验异常
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValidException(MethodArgumentNotValidException e) {
        return Result.error(400, "参数校验失败");
    }
    
    // 权限不足异常
    @ExceptionHandler(AccessDeniedException.class)
    public Result<Void> handleAccessDeniedException(AccessDeniedException e) {
        return Result.error(403, "权限不足");
    }
    
    // 未知异常
    @ExceptionHandler(Exception.class)
    public Result<Void> handleException(Exception e) {
        return Result.error(500, "系统内部错误");
    }
}
```

### 4.2 统一响应格式

```json
{
  "code": 200,
  "message": "操作成功",
  "data": { ... }
}
```

### 4.3 错误码定义

| 错误码 | 说明 |
|--------|------|
| 200 | 成功 |
| 400 | 参数错误 |
| 401 | 未授权/Token过期 |
| 403 | 权限不足 |
| 404 | 资源不存在 |
| 500 | 服务器内部错误 |

---

## 5. 核心业务流程

### 5.1 预订入住流程

```
┌─────────┐     ┌─────────┐     ┌─────────┐     ┌─────────┐
│  创建   │────▶│  确认   │────▶│  办理   │────▶│  退房   │
│  预订   │     │  预订   │     │  入住   │     │  结算   │
└─────────┘     └─────────┘     └─────────┘     └─────────┘
     │               │               │               │
     ▼               ▼               ▼               ▼
 状态: 0          状态: 1         状态: 2         状态: 3
 待确认           已确认          已入住          已完成
```

### 5.2 账单生成流程

```
入住登记 ──▶ 生成房费账单 ──▶ 收取押金
    │
    ▼
客房消费 ──▶ 生成消费账单
    │
    ▼
退房结算 ──▶ 汇总账单 ──▶ 退还押金 ──▶ 更新客户消费
```

---

## 6. 定时任务设计 (Quartz)

### 6.1 任务配置

| 任务名称 | Cron表达式 | 说明 |
|----------|------------|------|
| DailyReportJob | 0 0 1 * * ? | 每天凌晨1点生成日报表 |
| MonthlyReportJob | 0 0 2 1 * ? | 每月1号凌晨2点生成月报表 |

### 6.2 报表生成流程

```
定时触发 ──▶ 查询业务数据 ──▶ 统计汇总 ──▶ 保存报表
              │
              ├── 营收数据 (bill表)
              ├── 入住数据 (check_in表)
              ├── 预订数据 (reservation表)
              └── 客户数据 (customer表)
```

---

## 7. 安全设计

### 7.1 安全措施

| 安全项 | 实现方式 |
|--------|----------|
| 密码加密 | BCrypt 加密存储 |
| SQL注入防护 | MyBatis 参数化查询 |
| XSS防护 | 前端输入过滤 |
| CSRF防护 | JWT Token 验证 |
| 接口权限 | Spring Security + @PreAuthorize |

### 7.2 Token 安全策略

- Token 有效期: 24小时
- Token 存储: Redis 缓存
- Token 刷新: 登录时重新生成
- Token 失效: 登出时删除缓存

---

## 8. 部署架构

### 8.1 Docker 容器化部署

```yaml
services:
  mysql:      # 数据库服务
  redis:      # 缓存服务
  backend:    # 后端API服务
  frontend:   # 前端静态服务
```

### 8.2 端口映射

| 服务 | 容器端口 | 主机端口 |
|------|----------|----------|
| MySQL | 3306 | 3306 |
| Redis | 6379 | 6379 |
| Backend | 8080 | 8080 |
| Frontend | 80 | 80 |

---

## 9. 技术难点与解决方案

### 9.1 Swagger + SpringBoot 2.7 兼容性问题

**问题**: Springfox 3.0 与 Spring Boot 2.7 存在兼容性问题，导致 NullPointerException

**解决方案**: 添加 BeanPostProcessor 过滤 HandlerMapping

```java
@Bean
public static BeanPostProcessor springfoxHandlerProviderBeanPostProcessor() {
    return new BeanPostProcessor() {
        @Override
        public Object postProcessAfterInitialization(Object bean, String beanName) {
            if (bean instanceof WebMvcRequestHandlerProvider || 
                bean instanceof WebFluxRequestHandlerProvider) {
                customizeSpringfoxHandlerMappings(getHandlerMappings(bean));
            }
            return bean;
        }
    };
}
```

### 9.2 Redis 反序列化问题

**问题**: SimpleGrantedAuthority 无法从 Redis 反序列化

**解决方案**: 在 LoginUser 中使用 @JsonIgnore 忽略 authorities 字段，动态生成权限列表

### 9.3 中文乱码问题

**问题**: API 返回中文显示乱码

**解决方案**: 配置 Spring 编码和数据库字符集为 UTF-8

```yaml
server:
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true
```

---

## 10. 详细权限控制流程

### 10.1 登录认证流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              用户登录流程                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────┐    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐      │
│  │  前端   │───▶│ AuthController│───▶│ AuthService │───▶│UserDetails  │      │
│  │ 登录页  │    │   /login    │    │   login()   │    │ServiceImpl  │      │
│  └─────────┘    └─────────────┘    └──────┬──────┘    └──────┬──────┘      │
│       │                                   │                  │             │
│       │                                   ▼                  ▼             │
│       │                           ┌─────────────┐    ┌─────────────┐       │
│       │                           │ BCrypt验证  │    │ 查询用户    │       │
│       │                           │   密码     │◀───│ 角色权限    │       │
│       │                           └──────┬──────┘    └─────────────┘       │
│       │                                  │                                 │
│       │                                  ▼                                 │
│       │                           ┌─────────────┐                          │
│       │                           │  JwtUtils   │                          │
│       │                           │ 生成Token   │                          │
│       │                           └──────┬──────┘                          │
│       │                                  │                                 │
│       │                                  ▼                                 │
│       │                           ┌─────────────┐                          │
│       │                           │   Redis     │                          │
│       │                           │ 缓存用户信息 │                          │
│       │                           └──────┬──────┘                          │
│       │                                  │                                 │
│       ◀──────────────────────────────────┘                                 │
│   返回Token                                                                │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 10.2 请求鉴权流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              API请求鉴权流程                                 │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────┐    ┌─────────────────┐    ┌─────────────┐    ┌───────────┐    │
│  │  前端   │───▶│JwtAuthentication│───▶│  JwtUtils   │───▶│  Redis    │    │
│  │ 请求API │    │    Filter       │    │ 解析Token   │    │ 查询缓存  │    │
│  │ +Token  │    └────────┬────────┘    └──────┬──────┘    └─────┬─────┘    │
│  └─────────┘             │                    │                 │          │
│                          │                    ▼                 ▼          │
│                          │             ┌─────────────┐   ┌───────────┐     │
│                          │             │ Token有效?  │   │ 用户存在? │     │
│                          │             └──────┬──────┘   └─────┬─────┘     │
│                          │                    │                │           │
│                          │         ┌──────────┴────────────────┘           │
│                          │         │                                       │
│                          │         ▼                                       │
│                          │  ┌─────────────┐                                │
│                          │  │ 设置Security│                                │
│                          │  │   Context   │                                │
│                          │  └──────┬──────┘                                │
│                          │         │                                       │
│                          │         ▼                                       │
│                          │  ┌─────────────┐    ┌─────────────┐             │
│                          └─▶│ Controller  │───▶│  Service    │             │
│                             │  处理请求   │    │  业务逻辑   │             │
│                             └─────────────┘    └─────────────┘             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 10.3 权限校验代码示例

```java
// 1. JWT过滤器 - JwtAuthenticationFilter.java
@Override
protected void doFilterInternal(HttpServletRequest request, 
                                HttpServletResponse response, 
                                FilterChain filterChain) {
    // 从Header获取Token
    String token = request.getHeader("Authorization");
    if (StringUtils.hasText(token) && token.startsWith("Bearer ")) {
        token = token.substring(7);
        
        // 验证Token
        if (jwtUtils.validateToken(token)) {
            Long userId = jwtUtils.getUserIdFromToken(token);
            
            // 从Redis获取用户信息
            LoginUser loginUser = (LoginUser) redisTemplate
                .opsForValue().get("login:" + userId);
            
            if (loginUser != null) {
                // 设置认证信息到SecurityContext
                UsernamePasswordAuthenticationToken authentication = 
                    new UsernamePasswordAuthenticationToken(
                        loginUser, null, loginUser.getAuthorities());
                SecurityContextHolder.getContext()
                    .setAuthentication(authentication);
            }
        }
    }
    filterChain.doFilter(request, response);
}

// 2. 方法级权限控制
@PreAuthorize("hasAuthority('system:user:list')")
@GetMapping("/list")
public Result<List<SysUser>> list() {
    return Result.success(sysUserService.list());
}
```

---

## 11. 异常处理机制详解

### 11.1 异常处理架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              异常处理架构                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        GlobalExceptionHandler                        │   │
│  │                      (@RestControllerAdvice)                         │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│          ┌─────────────────────────┼─────────────────────────┐             │
│          │                         │                         │             │
│          ▼                         ▼                         ▼             │
│  ┌───────────────┐        ┌───────────────┐        ┌───────────────┐       │
│  │BusinessException│       │ValidException │        │ RuntimeException│     │
│  │   业务异常     │        │  参数校验异常  │        │   系统异常     │       │
│  └───────┬───────┘        └───────┬───────┘        └───────┬───────┘       │
│          │                        │                        │               │
│          ▼                        ▼                        ▼               │
│  ┌───────────────┐        ┌───────────────┐        ┌───────────────┐       │
│  │ Result.error  │        │ Result.error  │        │ Result.error  │       │
│  │ (code, msg)   │        │ (400, 参数错误)│        │ (500, 系统错误)│       │
│  └───────────────┘        └───────────────┘        └───────────────┘       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 11.2 异常类型定义

```java
// 1. 业务异常类 - BusinessException.java
public class BusinessException extends RuntimeException {
    private Integer code;
    private String message;
    
    public BusinessException(String message) {
        this.code = 500;
        this.message = message;
    }
    
    public BusinessException(Integer code, String message) {
        this.code = code;
        this.message = message;
    }
}

// 2. 全局异常处理器 - GlobalExceptionHandler.java
@RestControllerAdvice
public class GlobalExceptionHandler {
    
    private static final Logger log = LoggerFactory.getLogger(GlobalExceptionHandler.class);
    
    /**
     * 业务异常处理
     */
    @ExceptionHandler(BusinessException.class)
    public Result<Void> handleBusinessException(BusinessException e) {
        log.warn("业务异常: {}", e.getMessage());
        return Result.error(e.getCode(), e.getMessage());
    }
    
    /**
     * 参数校验异常 - @Valid注解触发
     */
    @ExceptionHandler(MethodArgumentNotValidException.class)
    public Result<Void> handleValidException(MethodArgumentNotValidException e) {
        String message = e.getBindingResult().getFieldErrors().stream()
            .map(FieldError::getDefaultMessage)
            .collect(Collectors.joining(", "));
        log.warn("参数校验失败: {}", message);
        return Result.error(400, message);
    }
    
    /**
     * 参数绑定异常
     */
    @ExceptionHandler(BindException.class)
    public Result<Void> handleBindException(BindException e) {
        String message = e.getBindingResult().getFieldErrors().stream()
            .map(FieldError::getDefaultMessage)
            .collect(Collectors.joining(", "));
        return Result.error(400, message);
    }
    
    /**
     * 权限不足异常
     */
    @ExceptionHandler(AccessDeniedException.class)
    public Result<Void> handleAccessDeniedException(AccessDeniedException e) {
        log.warn("权限不足: {}", e.getMessage());
        return Result.error(403, "权限不足，无法访问");
    }
    
    /**
     * 认证异常
     */
    @ExceptionHandler(AuthenticationException.class)
    public Result<Void> handleAuthenticationException(AuthenticationException e) {
        log.warn("认证失败: {}", e.getMessage());
        return Result.error(401, "认证失败，请重新登录");
    }
    
    /**
     * 数据库异常
     */
    @ExceptionHandler(DataAccessException.class)
    public Result<Void> handleDataAccessException(DataAccessException e) {
        log.error("数据库异常: ", e);
        return Result.error(500, "数据库操作失败");
    }
    
    /**
     * 未知异常兜底处理
     */
    @ExceptionHandler(Exception.class)
    public Result<Void> handleException(Exception e) {
        log.error("系统异常: ", e);
        return Result.error(500, "系统内部错误，请联系管理员");
    }
}
```

### 11.3 统一响应格式

```java
// Result.java - 统一响应封装
@Data
public class Result<T> {
    private Integer code;      // 状态码
    private String message;    // 提示信息
    private T data;            // 数据
    private Long timestamp;    // 时间戳
    
    public static <T> Result<T> success() {
        return success(null);
    }
    
    public static <T> Result<T> success(T data) {
        Result<T> result = new Result<>();
        result.setCode(200);
        result.setMessage("操作成功");
        result.setData(data);
        result.setTimestamp(System.currentTimeMillis());
        return result;
    }
    
    public static <T> Result<T> error(String message) {
        return error(500, message);
    }
    
    public static <T> Result<T> error(Integer code, String message) {
        Result<T> result = new Result<>();
        result.setCode(code);
        result.setMessage(message);
        result.setTimestamp(System.currentTimeMillis());
        return result;
    }
}
```

### 11.4 错误码规范

| 错误码 | 类型 | 说明 | 示例 |
|--------|------|------|------|
| 200 | 成功 | 请求成功 | 操作成功 |
| 400 | 客户端错误 | 参数错误 | 手机号格式不正确 |
| 401 | 认证错误 | 未登录或Token过期 | 请重新登录 |
| 403 | 权限错误 | 无权限访问 | 权限不足 |
| 404 | 资源错误 | 资源不存在 | 用户不存在 |
| 500 | 服务器错误 | 系统内部错误 | 系统繁忙 |

---

## 12. 数据库索引优化建议

### 12.1 现有索引

```sql
-- 用户表
UNIQUE KEY uk_username (username)

-- 房间表
UNIQUE KEY uk_room_number (room_number)
KEY idx_room_type (room_type_id)
KEY idx_status (status)

-- 预订表
UNIQUE KEY uk_reservation_no (reservation_no)
KEY idx_customer (customer_id)
KEY idx_check_in_date (check_in_date)
KEY idx_status (status)

-- 入住表
UNIQUE KEY uk_check_in_no (check_in_no)
KEY idx_customer (customer_id)
KEY idx_room (room_id)
KEY idx_check_in_time (check_in_time)
KEY idx_status (status)

-- 账单表
UNIQUE KEY uk_bill_no (bill_no)
KEY idx_check_in (check_in_id)
KEY idx_customer (customer_id)
KEY idx_create_time (create_time)
```

### 12.2 建议添加的索引

```sql
-- 预订表：按日期范围查询优化
ALTER TABLE reservation ADD INDEX idx_date_range (check_in_date, check_out_date);

-- 账单表：按类型和状态联合查询
ALTER TABLE bill ADD INDEX idx_type_status (bill_type, payment_status);

-- 客户表：按会员等级查询
ALTER TABLE customer ADD INDEX idx_member_level (member_level);

-- 日报表：按日期范围查询
ALTER TABLE daily_report ADD INDEX idx_date_range (report_date);
```

---

## 13. 前端代码注释规范

### 13.1 组件注释示例

```vue
<template>
  <!-- 预订列表页面 -->
  <div class="reservation-list">
    <!-- 搜索表单区域 -->
    <el-form :model="queryParams" inline>
      <!-- 预订状态筛选 -->
      <el-form-item label="状态">
        <el-select v-model="queryParams.status" placeholder="请选择">
          <el-option label="待确认" :value="0" />
          <el-option label="已确认" :value="1" />
        </el-select>
      </el-form-item>
    </el-form>
    
    <!-- 数据表格区域 -->
    <el-table :data="tableData" v-loading="loading">
      <!-- 表格列定义 -->
    </el-table>
  </div>
</template>

<script setup>
/**
 * 预订列表页面
 * @description 展示所有预订记录，支持筛选、分页、确认、取消等操作
 * @author Hotel Team
 * @date 2026-02-06
 */

import { ref, onMounted } from 'vue'
import { getReservationPage, confirmReservation } from '@/api/reservation'

// ==================== 响应式数据 ====================

/** 表格数据 */
const tableData = ref([])

/** 加载状态 */
const loading = ref(false)

/** 查询参数 */
const queryParams = ref({
  status: null,
  checkInDate: null,
  pageNum: 1,
  pageSize: 10
})

// ==================== 生命周期 ====================

onMounted(() => {
  fetchData()
})

// ==================== 方法定义 ====================

/**
 * 获取预订列表数据
 * @returns {Promise<void>}
 */
const fetchData = async () => {
  loading.value = true
  try {
    const res = await getReservationPage(queryParams.value)
    tableData.value = res.data.records
  } finally {
    loading.value = false
  }
}

/**
 * 确认预订
 * @param {number} id - 预订ID
 */
const handleConfirm = async (id) => {
  await confirmReservation(id)
  ElMessage.success('确认成功')
  fetchData()
}
</script>
```

### 13.2 后端代码注释示例

```java
/**
 * 预订服务实现类
 * <p>
 * 提供预订相关的业务逻辑处理，包括：
 * - 创建预订
 * - 确认预订
 * - 取消预订
 * - 预订转入住
 * </p>
 *
 * @author Hotel Team
 * @since 1.0.0
 */
@Service
@Transactional(rollbackFor = Exception.class)
public class ReservationService {

    /**
     * 创建预订
     * <p>
     * 业务流程：
     * 1. 生成预订单号
     * 2. 计算入住天数和总价
     * 3. 保存预订记录
     * 4. 更新房间状态（如已分配房间）
     * </p>
     *
     * @param request 预订请求参数
     * @return 预订ID
     * @throws BusinessException 当房间不可用时抛出
     */
    public Long createReservation(ReservationRequest request) {
        // 1. 检查房间可用性
        if (request.getRoomId() != null) {
            Room room = roomMapper.selectById(request.getRoomId());
            if (room.getStatus() != 0) {
                throw new BusinessException("该房间不可预订");
            }
        }
        
        // 2. 生成预订单号 (格式: RSV + 日期 + 4位序号)
        String reservationNo = generateReservationNo();
        
        // 3. 计算入住天数
        long nights = ChronoUnit.DAYS.between(
            request.getCheckInDate(), 
            request.getCheckOutDate()
        );
        
        // 4. 计算总价 = 房型单价 × 天数 × 房间数
        RoomType roomType = roomTypeMapper.selectById(request.getRoomTypeId());
        BigDecimal totalPrice = roomType.getPrice()
            .multiply(BigDecimal.valueOf(nights))
            .multiply(BigDecimal.valueOf(request.getRoomCount()));
        
        // 5. 构建预订实体并保存
        Reservation reservation = new Reservation();
        reservation.setReservationNo(reservationNo);
        reservation.setCustomerId(request.getCustomerId());
        reservation.setRoomTypeId(request.getRoomTypeId());
        reservation.setRoomId(request.getRoomId());
        reservation.setCheckInDate(request.getCheckInDate());
        reservation.setCheckOutDate(request.getCheckOutDate());
        reservation.setNights((int) nights);
        reservation.setTotalPrice(totalPrice);
        reservation.setStatus(0); // 待确认
        reservationMapper.insert(reservation);
        
        // 6. 更新房间状态为已预订
        if (request.getRoomId() != null) {
            roomMapper.updateStatus(request.getRoomId(), 1);
        }
        
        return reservation.getId();
    }
}
```
