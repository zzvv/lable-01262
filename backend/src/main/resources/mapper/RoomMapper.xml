<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hotel.mapper.RoomMapper">

    <resultMap id="RoomResultMap" type="com.hotel.entity.Room">
        <id property="id" column="id"/>
        <result property="roomNumber" column="room_number"/>
        <result property="roomTypeId" column="room_type_id"/>
        <result property="floor" column="floor"/>
        <result property="status" column="status"/>
        <result property="remark" column="remark"/>
        <result property="deleted" column="deleted"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <association property="roomType" javaType="com.hotel.entity.RoomType">
            <id property="id" column="rt_id"/>
            <result property="typeName" column="type_name"/>
            <result property="price" column="price"/>
            <result property="bedType" column="bed_type"/>
            <result property="maxGuests" column="max_guests"/>
            <result property="area" column="area"/>
        </association>
    </resultMap>

    <select id="selectRoomPage" resultMap="RoomResultMap">
        SELECT r.*, rt.id as rt_id, rt.type_name, rt.price, rt.bed_type, rt.max_guests, rt.area
        FROM room r
        LEFT JOIN room_type rt ON r.room_type_id = rt.id
        WHERE r.deleted = 0
        <if test="roomNumber != null and roomNumber != ''">
            AND r.room_number LIKE CONCAT('%', #{roomNumber}, '%')
        </if>
        <if test="roomTypeId != null">
            AND r.room_type_id = #{roomTypeId}
        </if>
        <if test="status != null">
            AND r.status = #{status}
        </if>
        <if test="floor != null">
            AND r.floor = #{floor}
        </if>
        ORDER BY r.room_number
    </select>

    <select id="selectRoomById" resultMap="RoomResultMap">
        SELECT r.*, rt.id as rt_id, rt.type_name, rt.price, rt.bed_type, rt.max_guests, rt.area
        FROM room r
        LEFT JOIN room_type rt ON r.room_type_id = rt.id
        WHERE r.id = #{id} AND r.deleted = 0
    </select>

</mapper>
